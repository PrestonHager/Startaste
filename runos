# shell script for creating os based on input filename
# arguments: runos [FILE]

FILE=$1

nasm -f bin -o bootloader.tmp.bin $FILE

dd status=noxfer conv=notrunc if=bootloader.tmp.bin of=bootloader.tmp.flp

qemu-system-i386 -fda bootloader.tmp.flp
